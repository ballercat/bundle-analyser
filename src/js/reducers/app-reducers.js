import {
  Map,
  List,
  fromJS
} from 'immutable';

import {
  curry
} from 'ramda';

const reducer = (state, action) {
  switch(action.type) {
    case FETCH_SCRIPT:
    default:
      return state;
  };
}

export default (defaults) => {

}

